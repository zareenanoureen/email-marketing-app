version: '3.9'
services:
  django:
    build:
      context: .
    command: >
      sh -c "python manage.py makemigrations && python.manage.py migrate && python manage.py runserver 0.0.0.0:80"
    volumes:
      - .:/aiemailmarketingapp
    ports:
      - "80:80"